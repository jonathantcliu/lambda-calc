structure Tests = struct

(* Note that you can now compile from source code *files* --
   see the sulc-code directory for some examples. If you would
   also like to write some unit tests in code, please do so
   here.
*)

end
